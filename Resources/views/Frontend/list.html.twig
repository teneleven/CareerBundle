{% import 'TenelevenCareerBundle:Frontend:macros.html.twig' as macros %}

{% block teneleven_careers_list %}
    {% if jobs.haveToPaginate()|default(false) %}
        {{ pagination(jobs) }}
    {% endif %}

    {{ macros.list(jobs) }}

    {% if jobs.haveToPaginate()|default(false) %}
        {{ pagination(jobs) }}
    {% endif %}
{% endblock %}